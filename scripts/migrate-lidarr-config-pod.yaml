apiVersion: v1
kind: Pod
metadata:
  name: migrate-lidarr-config
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: copier
      image: alpine:3.20
      command: ["/bin/sh", "-c"]
      args:
        - cd /src && tar cpf - . | (cd /dst && tar xpf -) && sync && echo copied-lidarr
      volumeMounts:
        - name: src
          mountPath: /src
        - name: dst
          mountPath: /dst
  volumes:
    - name: src
      persistentVolumeClaim:
        claimName: lidarr-config
    - name: dst
      persistentVolumeClaim:
        claimName: lidarr-config-ceph
