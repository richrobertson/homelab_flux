apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sonarr
spec:
  values:
    workload:
      main:
        replicas: 1
    persistence:
      downloads:
        size: 3000Gi
   