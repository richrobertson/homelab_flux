apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: syncthing
spec:
  suspend: false
  values:
    workload:
      main:
        replicas: 1
    persistence:
      downloads:
        enabled: true