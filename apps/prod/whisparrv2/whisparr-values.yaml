apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: whisparrv2
spec:
  values:
    image:
      repository: ghcr.io/hotio/whisparr
      pullPolicy: IfNotPresent
      tag: latest
    whisparr:
      api:
        general:
          baseUrl: https://whisparrv2.myrobertson.com
    workload:
      main:
        replicas: 1
    persistence:
      media:
        enabled: true
      downloads:
        enabled: true
   