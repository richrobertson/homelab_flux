apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plex
spec:
  interval: 1h
  values:
    pms:
      resources:
        requests:
          cpu: 500m
          memory: 1Gi
        limits:
          cpu: 12000m
          memory: 8Gi
