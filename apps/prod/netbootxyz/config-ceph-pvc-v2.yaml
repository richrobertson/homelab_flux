apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbootxyz-config-ceph-v2
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: csi-cephfs-sc
