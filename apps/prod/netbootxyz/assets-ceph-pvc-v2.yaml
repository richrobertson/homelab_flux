apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbootxyz-assets-ceph-v2
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: csi-cephfs-sc
