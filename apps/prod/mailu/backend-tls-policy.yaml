apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: mailu-front-https
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: mailu-front
      sectionName: https
  validation:
    hostname: mail.myrobertson.net
    wellKnownCACertificates: System
