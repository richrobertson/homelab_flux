apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns
  namespace: external-dns
spec:
  values:
    txtOwnerId: prod.myrobertson.net
    domainFilters: [ prod.myrobertson.net ]
