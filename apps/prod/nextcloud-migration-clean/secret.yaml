apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: nextcloud-migration-secret
spec:
  type: kv-v2
  mount: secret
  path: nextcloud/prod/app
  refreshAfter: 60s
  destination:
    create: true
    name: nextcloud-migration-secret
