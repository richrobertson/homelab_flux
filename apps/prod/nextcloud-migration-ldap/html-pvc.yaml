apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-migration-ldap-html
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: csi-cephfs-sc
  resources:
    requests:
      storage: 100Gi
