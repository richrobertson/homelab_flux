apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: nextcloud-oidc-secret
spec:
  type: kv-v2
  mount: secret
  path: nextcloud/staging/oidc
  refreshAfter: 60s
  destination:
    create: true
    name: nextcloud-oidc-secret
