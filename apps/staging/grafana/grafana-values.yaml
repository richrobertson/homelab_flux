apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana
spec:
  values:

    grafana:
      api:
        general:
          baseUrl: https://grafana.staging.myrobertson.net

   