apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bazarr
spec:
  values:

    bazarr:
      api:
        general:
          baseUrl: https://bazarr.staging.myrobertson.net
    workload:
      main:
        replicas: 0
