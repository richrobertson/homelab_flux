apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbootxyz-config-ceph
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-filesystem
