apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbootxyz-assets-ceph
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ceph-filesystem
