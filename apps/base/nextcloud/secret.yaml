apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: nextcloud-secret
spec:
  type: kv-v2
  mount: secret
  path: nextcloud/env/app
  refreshAfter: 60s
  destination:
    create: true
    name: nextcloud-secret
