apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: bazarr
spec:
  interval: 1h
  url: oci://oci.trueforge.org/truecharts/bazarr
  ref:
    semver: ">=22.x"
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
