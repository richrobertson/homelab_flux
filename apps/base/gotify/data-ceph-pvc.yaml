apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gotify-data-ceph
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-filesystem
