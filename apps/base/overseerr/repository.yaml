apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: overseerr
spec:
  interval: 1h
  url: oci://oci.trueforge.org/truecharts/overseerr
  ref:
    semver: ">=16.x"
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy