---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: seerr
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: seerr
  values:
    service:
      main:
        ports:
          main:
            port: 5055
    persistence:
      config:
        enabled: true
        storageClass: csi-cephfs-sc
        existingClaim: overseerr-config-ceph
        accessModes: ReadWriteMany
        targetSelector:
          main:
            main:
              mountPath: /app/config
            exportarr:
              mountPath: /app/config
              readOnly: true