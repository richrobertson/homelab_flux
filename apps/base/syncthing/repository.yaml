apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: syncthing
spec:
  interval: 1h
  url: oci://oci.trueforge.org/truecharts/syncthing
  ref:
    tag: "23.1.0"
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
