---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: syncthing
  namespace: syncthing
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: syncthing
    namespace: syncthing
  values:
    persistence:
      config:
        storageClass: synology-iscsi-storage