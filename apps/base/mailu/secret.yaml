apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: mailu-secret
spec:
  type: kv-v2
  mount: secret
  path: mailu/env/app
  refreshAfter: 60s
  destination:
    create: true
    name: mailu-secret
