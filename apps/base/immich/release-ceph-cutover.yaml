apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
spec:
  values:
    server:
      controllers:
        main:
          containers:
            main:
              env:
                DB_HOSTNAME: "cluster-immich-ceph-rw"
