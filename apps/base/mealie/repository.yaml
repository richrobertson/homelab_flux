apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: mealie
spec:
  interval: 1h
  url: oci://oci.trueforge.org/truecharts/mealie
  ref:
    semver: ">=26.x"
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy