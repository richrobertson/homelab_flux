apiVersion: apps/v1
kind: Deployment
metadata:
  name: nudge-worker
  labels:
    app.kubernetes.io/name: nudge-worker
    app.kubernetes.io/part-of: task-control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nudge-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nudge-worker
        app.kubernetes.io/part-of: task-control-plane
    spec:
      containers:
        - name: nudge-worker
          image: ghcr.io/richrobertson/nudge-worker:0.1.2
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: nudge-worker-config
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-openai
                  key: OPENAI_API_KEY
                  optional: true
            - name: VIKUNJA_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-vikunja
                  key: VIKUNJA_API_TOKEN
                  optional: true
            - name: NTFY_ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-app
                  key: NTFY_ACCESS_TOKEN
                  optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
