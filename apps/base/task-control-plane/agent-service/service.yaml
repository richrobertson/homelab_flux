apiVersion: v1
kind: Service
metadata:
  name: agent-service
  labels:
    app.kubernetes.io/name: agent-service
    app.kubernetes.io/part-of: task-control-plane
spec:
  selector:
    app.kubernetes.io/name: agent-service
  ports:
    - name: http
      port: 8080
      targetPort: http
