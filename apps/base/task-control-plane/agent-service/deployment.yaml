apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-service
  labels:
    app.kubernetes.io/name: agent-service
    app.kubernetes.io/part-of: task-control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent-service
        app.kubernetes.io/part-of: task-control-plane
    spec:
      containers:
        - name: agent-service
          image: ghcr.io/richrobertson/agent-service:0.3.11
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: agent-service-config
          env:
            - name: PYTHONPATH
              value: /app/src
            - name: TZ
              value: US/Pacific
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-openai
                  key: OPENAI_API_KEY
                  optional: true
            - name: VIKUNJA_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-vikunja
                  key: VIKUNJA_API_TOKEN
                  optional: true
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-cnpg-app
                  key: username
                  optional: true
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-cnpg-app
                  key: password
                  optional: true
            - name: POSTGRES_DATABASE
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-cnpg-app
                  key: dbname
                  optional: true
            - name: TELEGRAM_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-app
                  key: TELEGRAM_BOT_TOKEN
                  optional: true
            - name: TELEGRAM_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-app
                  key: TELEGRAM_WEBHOOK_SECRET
                  optional: true
            - name: TELEGRAM_PRIMARY_CHAT_ID
              valueFrom:
                secretKeyRef:
                  name: task-control-plane-app
                  key: TELEGRAM_PRIMARY_CHAT_ID
                  optional: true
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
