apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-config-ceph
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-filesystem
