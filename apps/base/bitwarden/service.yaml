apiVersion: v1
kind: Service
metadata:
  name: bitwarden
  namespace: default
spec:
  selector:
    app.kubernetes.io/name: bitwarden
  ports:
    - name: http
      port: 8080
      targetPort: http
