apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  #- cluster-issuers.yaml
  - rook-external-cluster.yaml
  - synology-csi.yaml
  - gotify-bridge.yml
  - ipaddresspool.yaml
spec:
  wait: true
    
