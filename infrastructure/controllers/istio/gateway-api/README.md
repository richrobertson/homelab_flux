# Gateway API

Gateway API resources used by Istio and ingress routing.

## Purpose

- Installs/maintains Gateway API definitions required by gateway resources.
- Provides versioned API artifacts for consistent cluster behavior.

## In this folder

- Versioned Gateway API manifest directories.

## Notes

- Keep versions explicit and verify compatibility with current Istio release.
