---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: istio-80-gateway
  namespace: default
spec:
  gatewayClassName: istio
  listeners:
  - name: http
    hostname: '*.myrobertson.com'
    protocol: HTTP
    port: 80
    allowedRoutes:
        namespaces:
          from: All