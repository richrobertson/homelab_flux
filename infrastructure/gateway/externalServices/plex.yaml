
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: plex-ext
  namespace: default
spec:
  endpoints:
  - address: 192.168.7.131
  hosts:
  - "plex.myrobertson.net"
  location: MESH_EXTERNAL
  ports:
  - name: http
    number: 32400
    protocol: HTTP
  resolution: STATIC

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plext-ext-route
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  parentRefs:
    - name:  myrobertson-com-gateway # Reference your Gateway resource
  hostnames:
    - "plex.myrobertson.com" # Hostname for which this route applies
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: "plex.myrobertson.net"
          port: 32400
          weight: 1
