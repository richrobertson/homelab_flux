
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: tautulli-ext
  namespace: default
spec:
  endpoints:
  - address: 192.168.1.215
  hosts:
  - "tautulli.myrobertson.net"
  location: MESH_EXTERNAL
  ports:
  - name: http
    number: 8181
    protocol: HTTP
  resolution: STATIC

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tautulli-ext-route
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  parentRefs:
    - name:  myrobertson-com-gateway # Reference your Gateway resource
  hostnames:
    - "tautulli.myrobertson.com" # Hostname for which this route applies
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: "tautulli.myrobertson.net"
          port: 8181
          weight: 1
