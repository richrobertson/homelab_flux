
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: overseerr-ext
  namespace: default
spec:
  endpoints:
  - address: 192.168.7.32
  hosts:
  - "overseerr.myrobertson.net"
  location: MESH_EXTERNAL
  ports:
  - name: http
    number: 5055
    protocol: HTTP
  resolution: STATIC

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: overseerr-ext-route
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  parentRefs:
    - name:  myrobertson-com-gateway # Reference your Gateway resource
  hostnames:
    - "overseerr.myrobertson.com" # Hostname for which this route applies
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: "overseerr.myrobertson.net"
          port: 5055
          weight: 1
