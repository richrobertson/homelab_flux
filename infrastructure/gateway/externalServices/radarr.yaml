
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: radarr-ext
  namespace: default
spec:
  endpoints:
  - address: 192.168.1.215
  hosts:
  - "radarr.myrobertson.net"
  location: MESH_EXTERNAL
  ports:
  - name: http
    number: 7878
    protocol: HTTP
  resolution: STATIC

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radarr-ext-route
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  parentRefs:
    - name:  myrobertson-com-gateway # Reference your Gateway resource
  hostnames:
    - "radarr.myrobertson.com" # Hostname for which this route applies
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: "radarr.myrobertson.net"
          port: 7878
          weight: 1
