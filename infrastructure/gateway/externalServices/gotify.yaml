
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: gotify-ext
  namespace: default
spec:
  endpoints:
  - address: 192.168.1.215
  hosts:
  - "gotify.myrobertson.net"
  location: MESH_EXTERNAL
  ports:
  - name: http
    number: 8180
    protocol: HTTP
  resolution: STATIC

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gotify-ext-route
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  parentRefs:
    - name:  myrobertson-com-gateway # Reference your Gateway resource
  hostnames:
    - "gotify.myrobertson.com" # Hostname for which this route applies
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: "gotify.myrobertson.net"
          port: 8180
          weight: 1
