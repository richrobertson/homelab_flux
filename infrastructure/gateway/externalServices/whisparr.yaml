
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: whisparr-ext
  namespace: default
spec:
  endpoints:
  - address: 192.168.1.215
  hosts:
  - "whisparr.myrobertson.net"
  location: MESH_EXTERNAL
  ports:
  - name: http
    number: 6969
    protocol: HTTP
  resolution: STATIC

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: whisparr-ext-route
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  parentRefs:
    - name:  myrobertson-com-gateway # Reference your Gateway resource
  hostnames:
    - "whisparr.myrobertson.com" # Hostname for which this route applies
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: "whisparr.myrobertson.net"
          port: 6969
          weight: 1
