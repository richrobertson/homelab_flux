
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: vault-ext
  namespace: default
spec:
  endpoints:
  - address: 192.168.7.130
  hosts:
  - "vault.myrobertson.net"
  location: MESH_EXTERNAL
  ports:
  - name: http
    number: 8200
    protocol: HTTP
  resolution: STATIC

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault-ext-route
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  parentRefs:
    - name:  myrobertson-com-gateway # Reference your Gateway resource
  hostnames:
    - "vault.myrobertson.com" # Hostname for which this route applies
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: "vault.myrobertson.net"
          port: 8200
          weight: 1
